create database school;      -- DATABASE 
use school;

create table student (          -- TABLE 1 
    Student_ID INT PRIMARY KEY,
    Student_Name VARCHAR(50),
    Age INT,
    Class char(10),
    Gender VARCHAR(10),
    City VARCHAR(50),
    Course_ID INT,
    Teacher_ID INT,
    Marks INT,
    Admission_Date DATE
);

 CREATE TABLE TEACHERS (          -- TABLE 2 
    Teacher_ID INT PRIMARY KEY,
    Teacher_Name VARCHAR(50),
    Subject VARCHAR(50),
    Salary INT,
    Experience INT
);

CREATE TABLE Courses (           -- TABLE 3
    Course_ID INT PRIMARY KEY,
    Course_Name VARCHAR(50),
    Fees INT
);

INSERT INTO Student VALUES       -- TABLE 1 VALUES 
(101,'Amit', 20 , 'XII','Male','Delhi', 1 , 201 , 85 ,'2025-01-10'),
(102,'Priya', 21 ,'XII','Female','Mumbai', 2 , 202 , 92 ,'2025-02-15'),
(103,'Rahul', 19 ,'XII','Male','Jaipur', 1 , 201 , 78 ,'2025-03-01'),
(104,'Neha', 20 ,'XII' ,'Female','Delhi', 3 , 203 , 88 ,'2025-01-20'),
(105,'Karan', 22 ,'XII','Male','Pune', 2 , 202 , 75 ,'2025-04-12'),
(106,'Riya', 21 ,'XII','Female','Jaipur', 3 , 203 , 95 ,'2025-02-25'),
(107,'Mohit', 20 ,'XII','Male','Delhi', 1 , 201 , 69 ,'2025-03-18'),
(108,'Anjali', 22 ,'XII','Female','Mumbai', 2 ,202 , 81 ,'2025-05-05');


SELECT 
    *
FROM
    student;

INSERT INTO Teachers VALUES      -- TABLE 2 VALUES
(201,'Rajesh','SQL',50000,5),
(202,'Sunita','Python',60000,8),
(203,'Anil','Power BI',55000,6),
(204,'Meena','Excel',45000,4);

SELECT 
    *
FROM
    teachers;

INSERT INTO Courses VALUES     -- TABLE 3 VALUES
(1,'SQL',12000),
(2,'Python',25000),
(3,'Power BI',10000),
(4,'Excel',5000);
select * from courses;

-- Display only student names and marks
SELECT 
    STUDENT_NAME, MARKS
FROM
    STUDENT; 

-- WHERE ALL QUESTION (MID LEVEL) 


-- Find students from Delhi    -- WHERE 
SELECT 
    STUDENT_NAME, CITY
FROM
    STUDENT
WHERE
    CITY = 'DELHI'; 
    
-- Find female students     -- WHERE 
SELECT 
    STUDENT_NAME, GENDER
FROM
    STUDENT
WHERE
    GENDER = 'FEMALE';

-- Show students with marks greater than 80  -- WHERE  
SELECT 
    STUDENT_NAME, MARKS
FROM
    STUDENT
WHERE
    MARKS > 80;


-- Show teachers whose salary is greater than 50,000   -- WHERE 
SELECT 
    TEACHER_NAME, SALARY
FROM
    TEACHERS
WHERE
    SALARY > 50000;    

--  Find courses with fees greater than 10,000   -- WHERE
SELECT 
    COURSE_NAME, FEES
FROM
    COURSES
WHERE
    FEES > 10000;

-- Display students ordered by marks (highest first) -- ORDER , LIMIT
SELECT 
    STUDENT_NAME, MARKS
FROM
    STUDENT
ORDER BY MARKS DESC
LIMIT 1;

-- Find students whose age is greater than 20  -- WHERE
SELECT 
    STUDENT_NAME, AGE
FROM
    STUDENT
WHERE
    AGE = 20;
    
-- Find teachers with more than 5 years of experience   -- WHERE 
SELECT 
    TEACHER_NAME, experience
FROM
    TEACHERS
WHERE
    experience > 5;

-- Show students whose city is Jaipur     -- WHERE
SELECT 
    STUDENT_NAME, CITY
FROM
    STUDENT
WHERE
    CITY = 'JAIPUR'; 

--  Aggregate Functions QUESTOINS

-- Find the highest marks
SELECT MAX(MARKS) FROM STUDENT;

-- Find the lowest marks
SELECT MIN(MARKS) FROM STUDENT;  

-- Find the average marks 
SELECT AVG(MARKS) FROM STUDENT;

-- Count total students
SELECT COUNT(*) FROM TEACHERS; 

-- Find total salary of all teachers
SELECT SUM(SALARY) FROM TEACHERS ; 

-- AND Operator Questions

-- Find students whose marks are greater than 80 AND city is 'Delhi'
SELECT STUDENT_NAME , MARKS , CITY
FROM STUDENT
WHERE MARKS > 80
AND 
CITY ='DELHI';
 
-- Find teachers whose salary is greater than 50,000 AND experience is more than 5 years 
SELECT TEACHER_NAME , SALARY , EXPERIENCE
FROM TEACHERS
WHERE SALARY > 50000 AND EXPERIENCE > 5;

-- Find students admitted after '2025-02-01' AND marks are greater than 75
 SELECT STUDENT_NAME , (Admission_Date) , MARKS 
 FROM STUDENT
 WHERE MARKS > 75 AND Admission_Date > '2025-02-01'
 ORDER BY Admission_Date asc , MARKS desc; 
 
 -- Find students from Jaipur AND enrolled in Course_ID = 3 WITH COURSE NAME 
 SELECT STUDENT_NAME , CITY , COURSE_NAME 
 FROM STUDENT
 JOIN COURSES
 ON STUDENT.COURSE_ID = COURSES.COURSE_ID 
 WHERE CITY ='JAIPUR' AND STUDENT.COURSE_ID = 3;
 
 -- OR Operator Questions 
 
--  Find students from Delhi OR Jaipur.
SELECT STUDENT_NAME , CITY 
FROM STUDENT
WHERE CITY = 'DELHI' OR 'JAIPUR';

SELECT STUDENT_NAME , CITY , MARKS
FROM STUDENT
WHERE CITY = 'JAIPUR' AND MARKS > 80;

SELECT STUDENT_NAME , CITY 
FROM STUDENT
WHERE CITY  IN('DELHI' , 'JAIPUR');

-- Find teachers whose subject is 'SQL' OR 'Python'
SELECT TEACHER_NAME , SUBJECT 
FROM TEACHERS
WHERE SUBJECT = 'SQL' OR 'PYTHON'; 

--  IN Operator Questions

-- Find students whose Course_ID is IN (1, 2)
 SELECT STUDENT_NAME , COURSE_ID 
 FROM STUDENT
 WHERE COURSE_ID IN(1,2);
 
 -- Find teachers whose subject is IN ('SQL', 'Python')
 SELECT TEACHER_NAME , SUBJECT 
 FROM TEACHERS 
 WHERE SUBJECT IN('SQL','PYTHON');
 
 -- Find courses whose fees are IN (5000, 10000, 12000)
 SELECT COURSE_NAME , FEES 
 FROM COURSES 
 WHERE FEES IN (5000, 10000, 12000);
 
 -- BETWEEN QUESTION 
 
 --  Find students whose marks are between 70 and 90
 SELECT STUDENT_NAME , MARKS 
 FROM STUDENT
 WHERE MARKS between 70 AND 90 ; 
 
 -- Find students admitted between '2025-01-01' and '2025-03-31'
 SELECT STUDENT_NAME , Admission_Date 
 FROM STUDENT 
 WHERE Admission_Date BETWEEN '2025-01-01' and '2025-03-31';
 
--  FIND STUDENTS AGE BETWEEN 18 AND 22.
 SELECT STUDENT_NAME, AGE 
 FROM STUDENT
 WHERE AGE  BETWEEN 18 AND 22;
 
 -- LIKE QUESTION 
 
 -- Find students whose names start with 'A'
 SELECT STUDENT_NAME 
 FROM STUDENT 
 where STUDENT_NAME like 'A%';
 
 -- Find students whose names END with 'A'
 SELECT STUDENT_NAME 
 FROM STUDENT 
 where STUDENT_NAME like '%A';

 -- Find students whose names CONTAIN 'A'
 SELECT STUDENT_NAME 
 FROM STUDENT 
 where STUDENT_NAME like '%A%';
 
 -- ORDER BY QUESTIONS
 
 -- Show students ordered by age
 SELECT STUDENT_NAME , AGE 
 FROM STUDENT 
 ORDER BY AGE;
 
 -- Show teachers ordered by salary (descending)
 SELECT TEACHER_NAME , SALARY
 FROM TEACHERS
 ORDER BY SALARY DESC;
 
 -- Show courses ordered by fees
 SELECT COURSE_NAME , FEES 
 FROM COURSES 
 ORDER BY FEES;
 
 -- Show students ordered by admission date
 SELECT STUDENT_NAME ,  admission_date
 FROM STUDENT
 ORDER BY admission_date;
 
 -- LIMIT QUESTIONS
 
 -- Display the top 3 students based on marks
 SELECT STUDENT_NAME , MARKS 
 FROM STUDENT 
 ORDER BY  MARKS DESC 
 LIMIT 3;
 
 --  Display the lowest 2 marks
  SELECT STUDENT_NAME , MARKS 
 FROM STUDENT 
 ORDER BY  MARKS ASC 
 LIMIT 2;
 
 -- Display the highest-paid teacher
 SELECT TEACHER_NAME AS highest_paid_teacher , SALARY
 FROM TEACHERS 
 ORDER BY  SALARY  DESC
 LIMIT 1;
 
 -- Display the cheapest course 
 SELECT *  
 FROM COURSES
 ORDER BY FEES ASC
 LIMIT 1;

-- GROUP BY QUESTIONS 

-- Count students in each course
SELECT COUNT(*) , COURSE_NAME
FROM STUDENT 
JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
GROUP BY COURSE_NAME; 

-- Find the maximum marks in each city

SELECT MAX(MARKS) , CITY 
FROM STUDENT
GROUP BY CITY ; 

-- Find the average marks of each city
SELECT AVG(MARKS) , CITY 
FROM STUDENT 
GROUP BY CITY;

--  Find the average age of students in each city
SELECT  AVG(AGE) , CITY 
FROM STUDENT
GROUP BY CITY;

-- Count male and female students separately 
SELECT GENDER , COUNT(*)
FROM STUDENT
GROUP BY GENDER; 

-- Find the total fees collected for each course
SELECT COURSE_NAME , COUNT(STUDENT_ID) AS TOTAL_STUDENT , FEES ,
COUNT(STUDENT_ID) * FEES AS TOTAL_COLLECTION
FROM STUDENT
JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
GROUP BY COURSE_NAME , FEES ;

-- Find the total marks obtained by students in each course
SELECT  COURSE_NAME , SUM(MARKS) AS TOTAL_MARKS 
FROM STUDENT
JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
GROUP BY COURSE_NAME;  

--  Find the total number of students in each city
SELECT COUNT(STUDENT_ID) , CITY 
FROM STUDENT
GROUP BY CITY ;

-- Find the total number of students under each teacher
SELECT TEACHER_NAME , COUNT(STUDENT_NAME) AS NUMBER_OF_STUDENT
FROM STUDENT
JOIN TEACHERS
ON STUDENT.TEACHER_ID = TEACHERS.TEACHER_ID
GROUP BY TEACHER_NAME;

-- Find the average salary by subject
SELECT AVG(SALARY) , SUBJECT 
FROM TEACHERS
GROUP BY SUBJECT; 

-- Find the average marks of male and female students separately
SELECT AVG(MARKS) , GENDER , COUNT(*) 
FROM STUDENT
GROUP BY GENDER;

-- Find the highest-paid teacher in each subject
SELECT TEACHER_NAME , MAX(SALARY) , SUBJECT 
FROM TEACHERS
GROUP BY TEACHER_NAME , SUBJECT 
ORDER BY TEACHER_NAME desc
LIMIT 1; 

-- Find the lowest marks in each course
SELECT MIN(MARKS) , COURSE_NAME 
FROM STUDENT
JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
GROUP BY COURSE_NAME;

-- Find the course with the maximum number of students.
SELECT COURSE_NAME , COUNT(*) AS MAX_STUDENTS
FROM STUDENT
JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
GROUP BY COURSE_NAME
LIMIT 1;

-- Count students under each teacher 
SELECT TEACHER_NAME  , COUNT(STUDENT_ID) AS TOTAL_STUDENT
FROM STUDENT
JOIN TEACHERS
ON STUDENT.TEACHER_ID = TEACHERS.TEACHER_ID
GROUP BY TEACHER_NAME;
 
 
 
-- HAVING QUESTIONS
-- DISPLAY CITIES HAVING MORE THAN 3 STUDENTS  
SELECT  CITY , COUNT(*)
FROM STUDENT
GROUP BY CITY  
having COUNT(*) > 2;

 -- DISPLAY COURSES HAVING MORE THAN 5 STUDENTS
 SELECT COURSE_NAME ,COUNT(*) AS NUMBER_OF_STUDENT
 FROM STUDENT
 JOIN COURSES
 ON STUDENT.COURSE_ID = COURSES.COURSE_ID
 GROUP BY COURSE_NAME 
 HAVING COUNT(*) > 2;
 
 -- SHOW COURSES WHOSE AVG MARKS ARE GREATER THAN 80
 SELECT COURSE_NAME , ROUND(AVG(MARKS),2) AS AVG_MARKS
FROM STUDENT
JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
GROUP BY COURSE_NAME 
HAVING AVG(MARKS) > 80 ; 
 
-- JOIN QUESTIONS

-- DISPLAY THE STUDENT NAME ALONG WITH THE COURSE NAME IN WITCH EACH STUDENT ISENROLLED  

SELECT STUDENT_NAME , COURSE_NAME 
FROM STUDENT
INNER JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
ORDER BY STUDENT_NAME ASC;

-- DESPLAY ALL COURSES ALONG WITH THE STUDENTS ENROLLED IN THEM , INCLUDING COURSES THAT HAVE NO STUDENTS. 
SELECT STUDENT_NAME , COURSE_NAME 
FROM STUDENT
LEFT JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
ORDER BY STUDENT_NAME ASC;

-- DISPLAY ALL COURSES ALONG WITH THE STUDENTS ENROLLED IN THEM, INCLUDING COURSES THAT HAVE NO STUDENTS 
SELECT STUDENT_NAME , COURSE_NAME 
FROM STUDENT
RIGHT JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
ORDER BY STUDENT_NAME ASC;

-- DISPLAY ALL STUDENTS AND ALL COURSES INCLUDING UNMATCHED RECORDS FROM BOTH TABLES
 
SELECT STUDENT_NAME , COURSE_NAME 
FROM STUDENT
LEFT JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID

UNION 

SELECT STUDENT_NAME , COURSE_NAME 
FROM STUDENT
RIGHT JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID
ORDER BY STUDENT_NAME ASC;


-- SUBQUERIES QUESTIONS

-- FIND STUDENTS WHO SCORED ABOVE THE AVG MARKS
SELECT STUDENT_NAME , AVG(MARKS)
FROM STUDENT
WHERE MARKS > (SELECT AVG(MARKS)
FROM STUDENT)
GROUP BY STUDENT_NAME; 

-- FIND STUDENTS WHOSE MARKS ARE GREATER THAN AMIT MARKS
SELECT *
FROM STUDENT 
WHERE MARKS > (SELECT  MARKS FROM STUDENT 
WHERE STUDENT_NAME = 'AMIT');

-- FIND STUDENTS ENROLLED IN THE SAME COURSE AS PRIYA
SELECT STUDENT_NAME , CLASS , MARKS , COURSE_NAME 
FROM STUDENT
JOIN COURSES
ON STUDENT.COURSE_ID = COURSES.COURSE_ID 
WHERE STUDENT.COURSE_ID = (SELECT STUDENT.COURSE_ID
FROM STUDENT
WHERE STUDENT_NAME = 'PRIYA') ;

-- FIND STUDENTS WHOSE MARKS ARE LOWER THAN THE HIGHEST MARKS
SELECT STUDENT_NAME , MARKS
FROM STUDENT
WHERE MARKS < (SELECT  MAX(MARKS) FROM STUDENT) 
ORDER BY MARKS DESC;

--  FIND TEACHERS WHOSE SALARY IS HIGHER THAN THE AVG SALARY 
SELECT TEACHER_NAME , SALARY 
FROM TEACHERS 
WHERE SALARY > (SELECT AVG(SALARY) FROM TEACHERS);
 
 -- FIND STUDENTS WHO SCORED THE SECOND HIGHEST MARKS
SELECT STUDENT_NAME , MARKS 
FROM STUDENT
WHERE MARKS = (SELECT MAX(MARKS) FROM STUDENT
WHERE MARKS < (SELECT MAX(MARKS) FROM STUDENT)
);






